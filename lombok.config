# make @NonNull throw IllegalArgumentException
lombok.nonNull.exceptionType = IllegalArgumentException

# tell lombok this is root directory
config.stopBubbling = true

# annotate generated code for coverage checks
lombok.addLombokGeneratedAnnotation = true
